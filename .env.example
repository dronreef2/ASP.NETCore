# Arquivo de exemplo para variáveis de ambiente no Railway
# Copie este arquivo para .env e configure os valores apropriados

# === BANCO DE DADOS ===
# DATABASE_URL será automaticamente configurado pelo Railway
# DATABASE_URL=postgresql://user:password@host:port/database

# === REDIS CACHE ===
# REDIS_URL será automaticamente configurado pelo Railway
# REDIS_URL=redis://user:password@host:port

# === JWT CONFIGURAÇÃO ===
JWT_SECRET_KEY=your-super-secret-jwt-key-here-change-this-in-production
JWT_ISSUER=TutorCopiloto
JWT_AUDIENCE=TutorCopiloto-Users

# === AI SERVICES ===
# OpenAI
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_BASE_URL=https://api.openai.com/v1

# Anthropic
ANTHROPIC_API_KEY=your-anthropic-api-key-here
ANTHROPIC_BASE_URL=https://api.anthropic.com

# LlamaIndex
LLAMAINDEX_API_KEY=your-llamaindex-api-key-here
LLAMAINDEX_BASE_URL=https://api.llamaindex.ai

# === GITHUB INTEGRATION ===
GITHUB_API_KEY=your-github-personal-access-token-here
GITHUB_WEBHOOK_SECRET=your-github-webhook-secret-here

# === MONITORAMENTO ===
# Railway fornece monitoramento integrado
# OTEL_EXPORTER_OTLP_ENDPOINT=https://otel.railway.app/v1/traces
# OTEL_SERVICE_NAME=tutor-copiloto-api

# === CORS CONFIGURAÇÃO ===
CORS_ALLOWED_ORIGINS=https://your-frontend-domain.com,https://*.railway.app

# === LOGGING ===
LOG_LEVEL=Information
SERILOG_MINIMUM_LEVEL=Information

# === RATE LIMITING ===
RATE_LIMIT_REQUESTS_PER_MINUTE=60
RATE_LIMIT_BURST_SIZE=10

# === CACHE CONFIGURAÇÃO ===
CACHE_EXPIRATION_MINUTES=30
REDIS_CONNECTION_TIMEOUT=5000

# === HEALTH CHECKS ===
HEALTH_CHECK_ENABLED=true
HEALTH_CHECK_TIMEOUT_SECONDS=10

# === NGROK (APENAS DESENVOLVIMENTO) ===
NGROK_ENABLED=false
NGROK_AUTH_TOKEN=your-ngrok-auth-token-here
NGROK_SUBDOMAIN=your-custom-subdomain

# === DEPLOYMENT ===
DEPLOYMENT_ENVIRONMENT=Production
DEPLOYMENT_VERSION=1.0.0

# === SECURITY ===
# API Keys para serviços externos
API_KEY_SALT=your-api-key-salt-here
ENCRYPTION_KEY=your-encryption-key-here

# === NOTIFICAÇÕES ===
# SMTP para notificações por email (opcional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_ENABLE_SSL=true

# === ANALYTICS ===
# Google Analytics ou similar (opcional)
GOOGLE_ANALYTICS_ID=GA_MEASUREMENT_ID

# === BACKUP ===
# Configurações de backup (Railway tem backup automático)
BACKUP_RETENTION_DAYS=30
BACKUP_SCHEDULE=daily

# === PERFORMANCE ===
# Configurações de performance
MAX_CONCURRENT_REQUESTS=100
REQUEST_TIMEOUT_SECONDS=30
MEMORY_CACHE_SIZE_MB=512
